language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-joomla-developer' ]; then cd .. && eval "mv $currentfolder generator-joomla-developer" && cd generator-joomla-developer; fi
